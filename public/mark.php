<?php
    require_once("../source/utilities.php");
    if(isset($_GET['as'], $_GET['item'])) {
        $as = $_GET['as'];
        $item = $_GET['item'];

        switch($as) {
            case 'done':
               $doneQuery = $conn
            break;
        }
    }